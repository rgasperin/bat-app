import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    textAlign: "center",
    color: "#E5BF3C",
    fontWeight: "bold",
    fontSize: 30
  },
  img: {
    resizeMode: "contain",
    height: 180
  }
});
